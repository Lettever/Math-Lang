require "./ast.cr"
require "./token.cr"
puts "TODO " + __FILE__

class Parser
    @tokens : Array(Token)
    @pos = 0

    def initialize(tokens : Array(tokens))
        @tokens = @tokens
    end

    def parse_program()
        
    end
end
